<!DOCTYPE html> <html  <head> <meta charset="UTF-8"> <title>Flores Amarillas - Manuel</title> <style>     body {         margin: 0;         height: 100vh;         background: linear-gradient(#87CEEB, #ffffff);         overflow: hidden;         display: flex;         justify-content: center;         align-items: center;         font-family: Arial, sans-serif;     }      h1 {         position: absolute;         color: #FFD700;         font-size: 60px;         text-shadow: 2px 2px 10px #00000050;         z-index: 2;     }      .flower {         position: absolute;         width: 40px;         height: 40px;         animation: float 10s linear infinite;     }      .petal {         width: 20px;         height: 20px;         background: yellow;         border-radius: 50%;         position: absolute;     }      .petal:nth-child(1) { top: 0; left: 10px; }     .petal:nth-child(2) { top: 10px; left: 0; }     .petal:nth-child(3) { top: 10px; left: 20px; }     .petal:nth-child(4) { top: 20px; left: 10px; }      .center {         width: 15px;         height: 15px;         background: orange;         border-radius: 50%;         position: absolute;         top: 12px;         left: 12px;     }      @keyframes float {         0% {             transform: translateY(100vh) rotate(0deg);             opacity: 1;         }         100% {             transform: translateY(-10vh) rotate(360deg);             opacity: 0;         }     } </style> </head> <body>  <h1>Manuel</h1>  <script>     function createFlower() {         const flower = document.createElement("div");         flower.classList.add("flower");          // posición horizontal aleatoria         flower.style.left = Math.random() * window.innerWidth + "px";          // duración aleatoria         flower.style.animationDuration = (5 + Math.random() * 5) + "s";          // pétalos         for (let i = 0; i < 4; i++) {             const petal = document.createElement("div");             petal.classList.add("petal");             flower.appendChild(petal);         }          // centro         const center = document.createElement("div");         center.classList.add("center");         flower.appendChild(center);          document.body.appendChild(flower);          // eliminar después de animación         setTimeout(() => {             flower.remove();         }, 10000);     }      setInterval(createFlower, 300); </script>  </body> </html>
